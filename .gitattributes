# Default: normalize line endings to LF on commit
* text=auto eol=lf

# Shell scripts must always be LF (CRLF breaks Linux containers)
*.sh text eol=lf

# Windows-only files keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
